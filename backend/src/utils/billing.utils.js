// backend/src/billing/billing.utils.js

// Example: Save invoice/subscription details in database
export const saveInvoiceToDB = async (session) => {
  // TODO: Replace with your DB logic (MongoDB / PostgreSQL / MySQL)
  console.log("💾 Saving invoice to DB:", session.id);
  return true;
};
